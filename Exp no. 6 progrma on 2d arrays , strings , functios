2D array :-

class MultidimensionalArray {
    public static void main(String[] args) {
        // create a 2d array   
        int[][] a = {
            {1, 2, 3}, 
            {4, 5, 6, 9}, 
            {7}, 
        };
      
        // calculate the length of each row
        System.out.println("Length of row 1: " + a[0].length);
        System.out.println("Length of row 2: " + a[1].length);
        System.out.println("Length of row 3: " + a[2].length);    
        }
}




MultidimensionalArray 

Source Code:
public class SingleDimensionArrayExample
{ public static void main(String[] args)
{
// 1. Declaration and Initialization of an Array
int[] numbers = new int[5]; // Creates an array to hold 5 integers

// 2. Assigning Values to the Array     	numbers[0] = 10;     	numbers[1] = 20;     	numbers[2] = 30;     	numbers[3] = 40;     	numbers[4] = 50;

// 3. Printing the Array
System.out.print("Array elements: ");
for (int i = 0; i < numbers.length; i++) {
System.out.print(numbers[i] + " ");
}
System.out.println();

// 4. Accessing Elements
System.out.println("First element: " + numbers[0]);
System.out.println("Last element: " + numbers[numbers.length - 1]);

// 5. Modifying Elements     
numbers[2] = 100; 
// Change the third element to 100
System.out.println("Modified third element: " + numbers[2]);

// 6. Iterating over the Array
System.out.println("Iterating over the array:");
for (int number : numbers) {
System.out.println(number);
}

// 7. Array Length
System.out.println("Array length: " + numbers.length); }
}
